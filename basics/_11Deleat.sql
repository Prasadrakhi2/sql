-- "deleate the particular row but the id to the next row will same not assigned the deleted id to the next row"

Syntex -> DELEATE FROM table_name [WHERE clouse]

ex-> 1.DELETE FROM example WHERE sn = 4
     2.DELETE FROM example WHERE name = "shubham"
     3.DELETE FROM example WHERE sn IN (6,8)
     "if we insert the value now than the id start from 9"
